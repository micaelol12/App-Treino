import { FeaturePlaceholder } from '@/shared/components/feature-placeholder';

export default function LoginRoute() {
  return (
    <FeaturePlaceholder
      title="Entrar"
      description="Acesso seguro ao seu histórico de treinos."
      nextStep="O fluxo de autenticação será conectado ao Firebase na fase de login."
    />
  );
}
