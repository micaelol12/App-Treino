import { FeaturePlaceholder } from '@/shared/components/feature-placeholder';

export default function WeightRoute() {
  return (
    <FeaturePlaceholder
      title="Peso"
      description="Veja sua evolução corporal."
      nextStep="O registro de peso será ligado ao mapper e às regras já testadas."
    />
  );
}
